package main

func main1() {
	// client := transport.GetClientChannel(&configuration.ClientChannelConfig{
	// 	Keep:    true,
	// 	Timeout: 10 * time.Second,
	// })
	// client.SetDecoder(&json.Decoder{})
	// client.SetEncoder(&json.Encoder{})
	// client.Send("http://localhost:8088/api/transport/request", []byte{})
}
